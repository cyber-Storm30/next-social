import React from "react";

const NotAuthorised = () => {
  return (
    <div
      style={{
        width: "100vw",
        height: "95vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      You are not authorised to access this content. Please login to access this
    </div>
  );
};

export default NotAuthorised;
